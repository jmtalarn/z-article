describe('Article router ', () => {
	it('calls articleStore find method when GET to /', () => {
		expect(true).toBe(false);
	});
	it('calls articleStore find method when GET to / with q parameter', () => {
		expect(true).toBe(false);
	});
	it('calls articleStore get method when GET to /:id', () => {
		expect(true).toBe(false);
	});
	it('returns 404 when GET to /:unknownid', () => {
		expect(true).toBe(false);
	});
});
